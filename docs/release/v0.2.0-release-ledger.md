# v0.2.0 Release Ledger

## Baseline

- Closeout branch: `codex/v0.2.0-closeout`.
- Phase 9 branch: `codex/repair-phase-9`.
- Phase 9 base commit: `2ff2db6` (`build: package tokenizer assets for device runtime`).
- Product version: `0.2.0+2`.
- Release tag: `v0.2.0`.
- AAR SHA-256: `9583871b4179ae48ce3c57796fad21580167de4183ca6c28efdc2ed4724f9b41`.
- Closeout verification date: `2026-08-10`.

## Closeout Evidence

- Flutter version preparation writes and verifies `flutter.versionName=0.2.0` and `flutter.versionCode=2`.
- Gradle fails closed when `android/local.properties` is missing, malformed, or still contains `1.0/1`.
- Debug APK, androidTest APK, release APK, and release AAB were built from one Gradle DAG.
- Shared `aapt` artifact audit verifies package/version/SDK/ABI, tokenizer assets, hidden MiniCPM paths, release native libraries, and manifest security attributes.
- Formal release signing remains disabled until `release-signing` secrets and environment are configured.

## Huawei Closeout Evidence

- Backup: `E:\Archive\Flutter\.note_secret_search_device_backups\20260810-150418Z`.
- Device gate: serial `H8B4C19731000256`, `HUAWEI / SPN-AL00 / API 29 / arm64-v8a`.
- Installed main package: `com.example.note_secret_search`, `versionName=0.2.0`, `versionCode=2`, certificate SHA-256 `8e0ddd3aba851740eb787e345b4df1cff4d60603f289549c53a916aac99ddead`.
- `ReleaseReadinessSmokeInstrumentationTest`: 1/1; `BgeEmbeddingRuntimeInstrumentationTest`: 3/3; `EmbeddingSensitiveRuntimeLogInstrumentationTest`: 1/1; `SensitiveRuntimeLogInstrumentationTest`: 1/1.
- The two user GGUF files remained unchanged: 2 files, total `877805024` bytes, identical SHA-256 inventory before and after.
- The instrumentation package was uninstalled, the temporary closeout model was removed, and cold-start/relaunch Logcat scanning found no crash signature or forbidden sensitive value.
- The pre-run Huawei package dump already reported `0.2.0+2`; this evidence does not claim a `v0.1.0 -> v0.2.0` migration.

## Final Local Gates

- `Invoke-QualityChecks.ps1 -PackageOnce`: passed; `flutter analyze`, 952 Dart tests, Android JVM tests, the single debug/androidTest/release APK/AAB Gradle DAG, and artifact audit all passed.
- `Invoke-LlmAarProvenanceGate.ps1 -SkipRebuild`: passed with the pinned AAR digest above.
- `Invoke-LlmAarProvenanceTests.ps1`: 6/6 tamper fixtures passed.
- `git diff --check`: passed.
- C: drive free space: `15.232 GiB` after clearing only the re-creatable Go build cache; Gradle and Pub caches remain on E: through Junctions.
- `C:\Users\Lenovo\.android\debug.keystore` was preserved in place and was not moved, deleted, or committed.

## Phase 8 Device Evidence Reused

- Device: Huawei SPN-AL00 / API 29.
- Commit: `2ff2db6`.
- `BgeEmbeddingRuntimeInstrumentationTest`: 3/3 passed.
- `EmbeddingSensitiveRuntimeLogInstrumentationTest`: 1/1 passed, logcat clean.
- `SensitiveRuntimeLogInstrumentationTest`: 1/1 passed, logcat clean.

## Artifact Invariants

- Tokenizer assets must be present for BGE and embed-min Chinese tokenizers.
- MiniCPM, mtmd, and mmproj entries must remain absent.
- `librnllama_v8.so` remains arm64-only through the pinned AAR.
- Release APK must not package Vulkan validation layers.
- Release manifest must keep cleartext, debuggable, and backup disabled.

## Expensive Gate Ledger

- Final full Dart tests: run once after all Phase 9 changes settle.
- Android JVM tests: run once after all Phase 9 changes settle.
- Android package DAG: run once for `:app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease :app:bundleRelease`.
- AAR provenance gate: run with `-SkipRebuild`; AAR rebuild requires separate approval.
- API 24/API 34 smoke jobs consume package-once artifacts.
- Huawei BGE/native sensitive-log suites are not repeated unless runtime source, AAR, native packaging, ABI, tokenizer/assets, relevant instrumentation, or dependencies materially change.

## Publish Blockers

- Non-clean release HEAD.
- `v0.2.0` tag/version mismatch.
- Signing certificate mismatch or missing historical upgrade evidence.
- AAR hash drift.
- Artifact audit failure for ABI, ORT, MiniCPM hidden assets, tokenizer assets, manifest, privacy, or native library policy.
- Release claims that exceed implemented behavior.
- Formal signing environment/secrets and public binary publication are still disabled.
